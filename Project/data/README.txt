The files that are formated 
                  beta_%0.5f.bin
in th bin/ directory, are created with 
              beta = 0.1 * (1.096 ^ n),
where n is in the range -26 - 99.

The file beta_1.00.bin is with 1e8 simulation steps.
